#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('publish', 'build the code and then publish to the npmjs.org')
  .parse(process.argv)
